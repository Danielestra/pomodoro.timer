{
  "name": "pomodoro-timer",
  "version": "1.0.0",
  "homepage": "https://github.com/Danielestra/pomodoro.timerr",  
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",   // Esse script prepara os arquivos para o deploy
    "deploy": "gh-pages -d build",  // Esse script envia os arquivos para o GitHub Pages
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}
