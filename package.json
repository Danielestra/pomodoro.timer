{
  "name": "pomodoro-timer",
  "version": "1.0.0",
  "homepage": "https://username.github.io/pomodoro-timer",  // Substitua "username" pelo seu nome de usuário no GitHub
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",  // Este script prepara os arquivos para o GitHub Pages
    "deploy": "gh-pages -d build",  // Este script envia a versão "build" para o GitHub Pages
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}
